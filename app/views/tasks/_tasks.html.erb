<% tasks.each do |task| %>
  <p>
    <strong><%= link_to task.title, task_path(task.id) %></strong>

    [<%= link_to "Delete", task_path(task.id), :method => :delete %>]
  </p>

  <p>

  </p>

<% end %>
