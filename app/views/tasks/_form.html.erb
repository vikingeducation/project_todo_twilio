
<%= form_for(@task) do |f| %>
  <%= f.label :date %>
  <%= f.text_field :date %><br>
  <%= f.label :description %>
  <%= f.text_area :description %>
  <p>Description must have at least 4 characters.</p>
  <%= f.submit %>
  <%end%>
