<%= form_for @user, @task do |task| %>

  <%= task.label "Tell us your task:" %><br>
  <%= task.text_field :todo, :class => "form-control" %><br>

  <%= task.submit "Submit", :class => "btn btn-primary form-control" %>

<% end %>
