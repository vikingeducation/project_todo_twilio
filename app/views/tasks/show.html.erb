<%= render partial: 'task_detail', locals: {task: @task} %>
<%= form_for @task, :method => "patch" do |f| %>
  <%= f.hidden_field :soft_delete, value: true%>
  <%= f.submit value: "Soft Delete"%>
<% end %>