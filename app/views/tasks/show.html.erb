<h1><%= @task.due %></h1>
<p>
  <%= @task.description %>
</p>

<p>
  <%= @task.additional %>
</p>

<br>
<%= link_to "Edit", @task %>
<%= link_to "Delete", @task, method: :delete,
    data: {confirm: "Really delete the article?"} %>
