<h1>INDEX</h1>

<% @tasks = @tasks.order(:date) %>
<% @tasks.each do | task | %>

<p>
<%= task.description %>----<%= link_to 'See Details', task_path( task.id ) %>----<%= link_to 'Delete', task_path( task.id ), method: "delete" %>----<%= link_to 'Edit Task', edit_task_path( task ) %>
</p>
<% end %>

