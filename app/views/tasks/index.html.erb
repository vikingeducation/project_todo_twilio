<%= form_for @new_task, :html => { :class => "form-group" } do |f| %>

  <%= f.text_field :description %>
  <%= f.date_field :due %>
  <%= f.submit "go" %>

<% end %>

<ul class="list-group">
  <% @tasks.each do |task| %>
    <%= render task %>
  <% end %>
</ul>
