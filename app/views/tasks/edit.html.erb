<h1>Edit task</h1>

<%= form_for @task do |f| %>
	<%= f.label :task %>
	<%= f.text_field :task %>

	<%= f.label :description %>
	<%= f.text_area :description %>

	<%= f.label :time_to_be_completed %>
	<%= f.text_field :time_to_be_completed %>

	<%= f.submit "Edit Task" %>
<% end %>	