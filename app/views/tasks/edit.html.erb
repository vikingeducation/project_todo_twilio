<h1> <%= "Edit Task #{@task.id}" %> </h1>

<%= form_for @task  do |f| %>
  <%= f.text_field :category %> <br>
  <%= f.text_area :description %> <br>
  <%= f.text_area :due_date %> <br>
  <%= f.submit "Update!" %>
<% end %>